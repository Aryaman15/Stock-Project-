import { Expectation, Instrument, MarketObservation, WatchlistItem } from '../models.js'; import type { ClockContext } from '../shared/clock.js'; import { AppError } from '../shared/errors.js'; import { expectedVsActual } from './presentation.js';
export const WATCH_AGENT_TOOLS=['SEARCH_INSTRUMENTS','GET_MARKET_SNAPSHOT','GET_WATCHLIST_ITEM','LIST_EXPECTATIONS','GET_EXPECTATION','PROPOSE_EXPECTATION','CREATE_CONFIRMED_EXPECTATION','GET_EXPECTED_VS_ACTUAL','GET_EXPECTATION_TIMELINE','GET_RETURN_BRIEF'] as const;
export type WatchAgentTool=typeof WATCH_AGENT_TOOLS[number];
export async function executeWatchTool(userId:string,clock:ClockContext,name:WatchAgentTool,args:any={}){if(name==='SEARCH_INSTRUMENTS'){const q=String(args.q??'').replace(/[.*+?^${}()|[\]\\]/g,'\\$&');return Instrument.find({active:true,$or:[{symbol:{$regex:`^${q}`,$options:'i'}},{name:{$regex:q,$options:'i'}}]}).limit(20).lean();}if(name==='GET_MARKET_SNAPSHOT')return MarketObservation.findOne({instrumentId:args.instrumentId,observedAt:{$lte:clock.asOf},availableAt:{$lte:clock.asOf}}).sort({observedAt:-1}).lean();if(name==='GET_WATCHLIST_ITEM')return WatchlistItem.findOne({_id:args.itemId}).lean();if(name==='LIST_EXPECTATIONS')return Expectation.find({userId,watchlistId:args.watchlistId}).lean();if(name==='GET_RETURN_BRIEF')return Expectation.find({userId}).lean();const e:any=await Expectation.findOne({_id:args.expectationId,userId}).lean();if(!e)throw new AppError(404,'EXPECTATION_NOT_FOUND','Expectation not found');if(name==='GET_EXPECTATION')return e;if(name==='GET_EXPECTED_VS_ACTUAL')return expectedVsActual(e);if(name==='GET_EXPECTATION_TIMELINE')return {expectationId:e._id,clock};throw new AppError(400,'AGENT_TOOL_UNAVAILABLE','This agent tool requires explicit confirmation through its dedicated endpoint');}
