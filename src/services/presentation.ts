export function expectedVsActual(e:any){const p=e.parameters;const m=e.resolution?.measurement??{};let expected:string;if(e.template==='RELATIVE_OUTPERFORMANCE')expected=`Outperform benchmark by at least ${p.minimumOutperformancePct}%`;else if(e.template==='PRICE_THRESHOLD')expected=`Close ${p.direction.toLowerCase()} ${p.threshold}`;else expected=`When benchmark gains ${p.benchmarkTriggerPct}%, participate by at least ${p.minimumParticipationRatio}`;const actual={instrumentReturnPct:m.instrumentReturnPct,benchmarkReturnPct:m.benchmarkReturnPct,relativePerformancePct:m.relativePerformancePct,participationRatio:m.participationRatio,startPrice:m.startPrice,endPrice:m.endPrice};const signalTypes:string[]=[];if(e.template==='BENCHMARK_PARTICIPATION'&&e.resolution?.outcome==='DISCONFIRMED')signalTypes.push('SILENT_STORY');if(e.status==='UNSCOREABLE')signalTypes.push('UNEXPLAINED');return {expected,actual,outcome:e.resolution?.outcome??null,status:e.status,evidence:e.resolution?.evidence??null,signalTypes};}
